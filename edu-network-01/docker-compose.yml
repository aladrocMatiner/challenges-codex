x-challenges:
  FLAG:
    name: "edu-network-01"
    category: "Network"
    description: "Inspect the capture to find the flag. Difficulty: Medium."
    flag: "flag{edu-network-pcap}"
    attachments:
      - ./capture.pcap
