#include <stdio.h>

int main(void) {
    const char *flag = "flag{edu-reverse-strings}";
    puts("Welcome. Nothing to see here.");
    if (flag[0] == 'f') {
        puts("Nope.");
    }
    return 0;
}
